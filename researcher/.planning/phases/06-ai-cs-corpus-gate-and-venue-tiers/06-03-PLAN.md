---
phase: 06-ai-cs-corpus-gate-and-venue-tiers
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - pipeline/corpus_gate.py
  - scripts/s2_corpus_gate.py
  - tests/test_corpus_gate.py
  - tests/fixtures/corpus_gate/expected_gate_decisions.jsonl
autonomous: true
requirements:
  - CORPUS-03
must_haves:
  truths:
    - "User can inspect one decision record for every staged work and see exactly why it was included or excluded."
    - "Newly ingested papers are handled by running a new gate pass against the new parent OpenAlex run, preserving older gate outputs."
    - "Venue-table changes are traceable through recorded fingerprints instead of silent in-place mutations."
  artifacts:
    - path: "data/runs/<gate_run>/corpus_gate/gate_decisions.jsonl"
      provides: "Per-work inclusion or exclusion decisions for the full parent run"
      contains: "\"decision\""
    - path: "pipeline/corpus_gate.py"
      provides: "Decision-log writer and lineage-safe rerun behavior"
      contains: "gate_decisions"
    - path: "tests/test_corpus_gate.py"
      provides: "Offline verification for full decision coverage and append-only reruns"
      contains: "decision"
  key_links:
    - from: "pipeline/corpus_gate.py"
      to: "data/runs/<gate_run>/corpus_gate/gate_decisions.jsonl"
      via: "one serialized decision per upstream work envelope"
      pattern: "reason_codes"
    - from: "scripts/s2_corpus_gate.py"
      to: "data/runs/<openalex_run>/openalex/works_raw.jsonl"
      via: "new gate run is always keyed by `parent_run_id` and `venue_table_hash`"
      pattern: "parent_run_id"
---

<objective>
Emit the full include/exclude decision log and lock the append-only rerun contract.

Purpose: Satisfy `CORPUS-03` so downstream ranking and export phases can prove why each paper was
in or out of the AI/CS corpus, even as new raw ingest runs arrive.
Output: `gate_decisions.jsonl`, stable reason-code stamping, and tests that prove append-only
lineage behavior.
</objective>

<execution_context>
@/Users/adam/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-RESEARCH.md
@.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-01-SUMMARY.md
@.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-02-SUMMARY.md
@pipeline/corpus_gate.py
@scripts/s2_corpus_gate.py
@pipeline/run_context.py

<interfaces>
Expected from `pipeline/corpus_gate.py` after `06-02`:

```python
def run_corpus_gate(...)-> GateRunResult: ...
```

Expected gate output contract after `06-02`:

```text
data/runs/<gate_run_id>/corpus_gate/
  ├── run.json
  └── gated_works.jsonl
```

This plan extends that contract. It must not replace it with a second storage model, mutate
existing gate runs in place, or introduce seen-ID state for Phase 6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit one decision record for every staged work</name>
  <files>
    pipeline/corpus_gate.py
    scripts/s2_corpus_gate.py
    tests/test_corpus_gate.py
    tests/fixtures/corpus_gate/expected_gate_decisions.jsonl
  </files>
  <read_first>
    .planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-RESEARCH.md
    .planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-02-SUMMARY.md
    pipeline/corpus_gate.py
    scripts/s2_corpus_gate.py
    pipeline/venue_tiers.py
  </read_first>
  <behavior>
    - Test 1: Every upstream work envelope yields exactly one record in `gate_decisions.jsonl`, regardless of whether it was included or excluded.
    - Test 2: Decision records include `gate_run_id`, `parent_run_id`, `source_record_id`, `openalex_source_id`, `decision`, `reason_codes`, `venue_table_hash`, and matched venue metadata when present.
    - Test 3: Excluded works do not leak into `gated_works.jsonl`, but they remain fully visible in the decision log with Phase 6 reason codes only.
  </behavior>
  <action>
    Extend `pipeline/corpus_gate.py` and `scripts/s2_corpus_gate.py` so each run also writes
    `data/runs/<gate_run_id>/corpus_gate/gate_decisions.jsonl`. The file must contain one decision
    record per upstream staged work with `decision=include|exclude`, `reason_codes`,
    `gate_run_id`, `parent_run_id`, `source_record_id`, `openalex_work_id`, `openalex_source_id`,
    `openalex_source_display_name`, `normalized_tier`, and matched venue row details when a row
    exists.

    Keep the reason set closed over the constants defined in `06-01`. Do not introduce a separate
    exclusions file, do not hide excluded records, and do not duplicate ranking logic inside the
    decision log. Update `tests/test_corpus_gate.py` first and store a deterministic expected
    decision fixture under `tests/fixtures/corpus_gate/expected_gate_decisions.jsonl`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_corpus_gate.py -q -k "decision_log or reason_codes"` passes.
    - `rg 'gate_decisions.jsonl|decision|reason_codes|venue_table_hash' pipeline/corpus_gate.py scripts/s2_corpus_gate.py` finds the decision-log contract.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_corpus_gate.py -q -k "decision_log or reason_codes"</automated>
  </verify>
  <done>
    The gate produces a complete, inspectable decision log for the full parent run, and included
    records remain a strict subset of that log.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock append-only rerun semantics for new parent runs and venue-table revisions</name>
  <files>
    pipeline/corpus_gate.py
    scripts/s2_corpus_gate.py
    tests/test_corpus_gate.py
  </files>
  <read_first>
    .planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-02-SUMMARY.md
    pipeline/corpus_gate.py
    scripts/s2_corpus_gate.py
    pipeline/run_context.py
    tests/test_corpus_gate.py
  </read_first>
  <behavior>
    - Test 1: Running the gate against a different parent OpenAlex run creates a new gate run and leaves prior gate outputs unchanged.
    - Test 2: Changing the venue-table content changes the recorded `venue_table_hash` and results in a new append-only gate run rather than mutating older decisions.
    - Test 3: The Phase 6 incremental contract is only append-only parent lineage plus recorded asset version; no seen-ID or in-place rewrite layer is introduced.
  </behavior>
  <action>
    Lock the Phase 6 operational contract in code and tests: newly ingested papers are handled by
    running `s2_corpus_gate.py` against the new upstream OpenAlex run, which creates a new
    `gate_run_id` with the new `parent_run_id`. Venue-table changes are handled the same way:
    record the new `venue_table_hash` in the manifest and decision log, write a new gate run, and
    leave the old gate run intact.

    Keep this deliberately narrow. Do not add an incremental seen-ID state file, do not mutate old
    `gated_works.jsonl` or `gate_decisions.jsonl`, and do not collapse multiple parent runs into one
    merged Phase 6 artifact. The append-only lineage is the entire incremental contract for this
    phase.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_corpus_gate.py -q -k "rerun_lineage or venue_table_hash"` passes.
    - `rg 'parent_run_id|venue_table_hash|included_count|excluded_count' pipeline/corpus_gate.py scripts/s2_corpus_gate.py pipeline/run_context.py` finds the rerun lineage fields.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_corpus_gate.py -q -k "rerun_lineage or venue_table_hash"</automated>
  </verify>
  <done>
    Phase 6 handles new raw ingest and venue-table revisions through new append-only gate runs with
    explicit lineage, without any hidden mutation or heuristic state layer.
  </done>
</task>

</tasks>

<verification>
Run `.venv/bin/python -m pytest tests/test_corpus_gate.py -q` and confirm the decision log covers
every upstream work while rerun tests prove append-only lineage behavior.
</verification>

<success_criteria>
Every staged OpenAlex work now receives a visible include or exclude decision, and Phase 6 can be
rerun safely for new parent runs or venue-table revisions without rewriting earlier outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-03-SUMMARY.md`
</output>
