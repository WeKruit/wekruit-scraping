---
phase: 06-ai-cs-corpus-gate-and-venue-tiers
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - pipeline/run_context.py
  - pipeline/corpus_gate.py
  - scripts/s2_corpus_gate.py
  - tests/test_corpus_gate.py
  - tests/fixtures/corpus_gate/works_raw.jsonl
autonomous: true
requirements:
  - CORPUS-01
must_haves:
  truths:
    - "User can run one corpus-gate stage against a staged OpenAlex run and receive only AI/CS works whose venues pass the reviewed table."
    - "Each gate run preserves lineage back to its parent OpenAlex run and records the exact venue-table version used."
    - "Corpus inclusion is driven only by the venue-table contract, not by citation counts, author signals, or fuzzy venue-name matching."
  artifacts:
    - path: "pipeline/run_context.py"
      provides: "Derived-stage manifest helper for lineage-safe corpus-gate runs"
      contains: "parent_run_id"
    - path: "pipeline/corpus_gate.py"
      provides: "Corpus-gate engine over staged OpenAlex `works_raw.jsonl`"
      contains: "run_corpus_gate"
    - path: "scripts/s2_corpus_gate.py"
      provides: "Executable CLI for append-only gate runs"
      contains: "argparse"
  key_links:
    - from: "data/runs/<openalex_run>/openalex/works_raw.jsonl"
      to: "pipeline/corpus_gate.py"
      via: "exact lookup of `raw.primary_location.source.id` against the reviewed asset"
      pattern: "primary_location.*source.*id"
    - from: "scripts/s2_corpus_gate.py"
      to: "pipeline/run_context.py"
      via: "gate run manifest records `parent_run_id`, `venue_table_hash`, and output paths"
      pattern: "venue_table_hash"
---

<objective>
Implement the append-only corpus gate over staged OpenAlex work records.

Purpose: Satisfy `CORPUS-01` by turning the reviewed venue-tier asset into a deterministic
`gated_works.jsonl` output with parent-run lineage retained.
Output: A derived-run manifest helper, corpus-gate engine, CLI entrypoint, and offline gate tests.
</objective>

<execution_context>
@/Users/adam/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-RESEARCH.md
@.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-01-SUMMARY.md
@pipeline/run_context.py
@pipeline/raw_staging.py
@scripts/s1_openalex_fetch.py
@sources/openalex.py

<interfaces>
Available Phase 1 helpers:

```python
def stage_records(path: Path | str, records: Iterable[dict[str, Any]], *, mirror_path: Path | str | None = None) -> Path: ...
```

Expected from `pipeline/venue_tiers.py` after `06-01`:

```python
def load_venue_tiers(path: Path | str) -> dict[str, VenueTierRow]: ...
def hash_venue_tier_asset(path: Path | str) -> str: ...
INCLUDE_REASON_CODES = ("matched_ai_cs_venue_tier",)
EXCLUDE_REASON_CODES = (
    "missing_primary_source",
    "source_not_in_ai_cs_table",
    "venue_row_unresolved",
    "venue_explicitly_excluded",
)
```

Use those contracts directly. Do not add `corpus_gate` to the source registry and do not mutate
Phase 1 raw runs in place.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a derived-run manifest contract for corpus-gate outputs</name>
  <files>
    pipeline/run_context.py
    tests/test_corpus_gate.py
  </files>
  <read_first>
    .planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-01-SUMMARY.md
    pipeline/run_context.py
    pipeline/incremental_state.py
    scripts/s1_openalex_fetch.py
  </read_first>
  <behavior>
    - Test 1: The new manifest helper creates an append-only child run with `stage_name`, `run_id`, `parent_run_id`, input paths, output paths, and asset fingerprints.
    - Test 2: Existing Phase 1 source-run behavior remains unchanged; the new helper is additive and used only for derived stages such as `corpus_gate`.
    - Test 3: The manifest surface records `included_count` and `excluded_count` so the corpus gate does not need a second reporting contract.
  </behavior>
  <action>
    Extend `pipeline/run_context.py` with a minimal derived-stage manifest helper for non-source
    stages. It must preserve the existing Phase 1 source-run contract untouched while adding a
    second manifest shape for child runs with `stage_name`, `run_id`, `parent_run_id`,
    `input_paths`, `output_paths`, `asset_fingerprints`, `included_count`, `excluded_count`,
    `created_at`, `completed_at`, and `status`.

    Use this helper for Phase 6 instead of inventing an ad-hoc JSON blob inside the CLI. The
    manifest must live under the new gate run directory, not inside the parent OpenAlex run, and it
    must keep the raw OpenAlex outputs append-only. Write `tests/test_corpus_gate.py` first and
    keep the helper generic enough for derived stages without backfilling the source registry.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_corpus_gate.py -q -k "derived_manifest or lineage"` passes.
    - `rg 'stage_name|parent_run_id|asset_fingerprints|included_count|excluded_count' pipeline/run_context.py` finds the new manifest fields.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_corpus_gate.py -q -k "derived_manifest or lineage"</automated>
  </verify>
  <done>
    Phase 6 has one lineage-safe derived-run manifest contract that fits the existing file-backed
    run model without pretending the gate is a new upstream source.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the strict corpus-gate engine and CLI over `works_raw.jsonl`</name>
  <files>
    pipeline/corpus_gate.py
    scripts/s2_corpus_gate.py
    tests/test_corpus_gate.py
    tests/fixtures/corpus_gate/works_raw.jsonl
  </files>
  <read_first>
    .planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-RESEARCH.md
    .planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-01-SUMMARY.md
    pipeline/raw_staging.py
    scripts/s1_openalex_fetch.py
    pipeline/venue_tiers.py
  </read_first>
  <behavior>
    - Test 1: Running the CLI with `--input-run <openalex_run>` and the reviewed venue table writes only included work envelopes to `gated_works.jsonl`.
    - Test 2: Inclusion is driven only by exact `raw.primary_location.source.id` matches against the venue table; missing or unmatched source IDs are excluded.
    - Test 3: The gated output preserves parent lineage and stamps the resolved venue row slug and normalized tier onto each included record.
  </behavior>
  <action>
    Create `pipeline/corpus_gate.py` and `scripts/s2_corpus_gate.py` as the single Phase 6 runtime.
    The CLI must accept `--input-run`, `--venue-table` (default
    `data/assets/ai_cs_venue_tiers.csv`), `--output-root`, and an optional deterministic `--run-id`
    for tests. It must read `data/runs/<input_run>/openalex/works_raw.jsonl`, inspect
    `record["raw"]["primary_location"]["source"]["id"]`, and join that value exactly against the
    reviewed venue table.

    For included records only, write `data/runs/<gate_run_id>/corpus_gate/gated_works.jsonl`.
    Preserve the original staged envelope and add only Phase 6 gate metadata such as `gate_run_id`,
    `parent_run_id`, `venue_row_slug`, `normalized_tier`, and a small `corpus_gate` metadata block.
    Reuse the existing append-only JSONL staging pattern. Do not add fuzzy display-name matching, do
    not query CCF/CORE at runtime, and do not gate on citations, author signals, or recency.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_corpus_gate.py -q -k "gate_cli or included_works"` passes.
    - `python scripts/s2_corpus_gate.py --help >/dev/null` exits successfully.
    - `rg 'gated_works.jsonl|venue_row_slug|normalized_tier|parent_run_id' pipeline/corpus_gate.py scripts/s2_corpus_gate.py` finds the expected output contract.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_corpus_gate.py -q -k "gate_cli or included_works" && python scripts/s2_corpus_gate.py --help >/dev/null</automated>
  </verify>
  <done>
    The repo can derive an AI/CS-only work subset from a staged OpenAlex run through one strict,
    append-only corpus-gate stage with lineage preserved.
  </done>
</task>

</tasks>

<verification>
Run `.venv/bin/python -m pytest tests/test_corpus_gate.py -q` and confirm the corpus gate writes
only included works and records parent lineage without any network dependency.
</verification>

<success_criteria>
Phase 6 can now take an existing OpenAlex run and deterministically emit one gated AI/CS work set
using only the reviewed venue table and exact source-ID joins.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-02-SUMMARY.md`
</output>
