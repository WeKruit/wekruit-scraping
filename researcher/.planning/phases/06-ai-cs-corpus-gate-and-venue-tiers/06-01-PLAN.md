---
phase: 06-ai-cs-corpus-gate-and-venue-tiers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/assets/ai_cs_venue_tiers.csv
  - pipeline/venue_tiers.py
  - tests/test_venue_tiers.py
autonomous: true
requirements:
  - CORPUS-02
must_haves:
  truths:
    - "User can open one local AI/CS venue-tier table and inspect upstream grades, normalized tier, inclusion flag, and last-reviewed metadata."
    - "Venue gating is keyed by an exact OpenAlex source-ID join, not venue-name heuristics."
    - "Explicitly excluded or unresolved venues remain visible in the asset instead of being hidden behind runtime fallbacks."
  artifacts:
    - path: "data/assets/ai_cs_venue_tiers.csv"
      provides: "Repo-owned AI/CS venue-tier asset keyed by exact OpenAlex source IDs"
      contains: "openalex_source_id"
    - path: "pipeline/venue_tiers.py"
      provides: "Venue-tier row model, CSV loader, validation, reason codes, and asset fingerprint helper"
      contains: "load_venue_tiers"
    - path: "tests/test_venue_tiers.py"
      provides: "Fixture-backed contract checks for the asset and loader"
      contains: "test_"
  key_links:
    - from: "data/assets/ai_cs_venue_tiers.csv"
      to: "pipeline/venue_tiers.py"
      via: "CSV loader validates and indexes rows by exact `openalex_source_id`"
      pattern: "openalex_source_id"
    - from: "data/runs/<openalex_run>/openalex/works_raw.jsonl"
      to: "data/assets/ai_cs_venue_tiers.csv"
      via: "`raw.primary_location.source.id` exact join key"
      pattern: "primary_location.*source.*id"
---

<objective>
Define the single reviewed AI/CS venue-tier asset and strict join contract for Phase 6.

Purpose: Satisfy `CORPUS-02` first so the corpus gate has one repo-owned source of truth keyed to
staged OpenAlex work records instead of heuristics.
Output: A reviewed venue-tier CSV, a strict loader/validator, stable reason-code constants, and
offline contract tests.
</objective>

<execution_context>
@/Users/adam/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-RESEARCH.md
@pipeline/raw_staging.py
@scripts/s1_openalex_fetch.py
@sources/openalex.py

<interfaces>
Phase 1 keeps source-native OpenAlex payloads under `raw`:

```python
def build_raw_envelope(..., raw: dict[str, Any], ...) -> dict[str, Any]:
    return {
        ...,
        "raw": raw,
    }
```

Corpus-gate join key must come from the staged OpenAlex payload itself:

```python
work["raw"]["primary_location"]["source"]["id"]
work["raw"]["primary_location"]["source"]["display_name"]
```

Do not add any runtime venue-name matching layer. The reviewed asset is the only gate input beyond
the staged OpenAlex run.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the AI/CS venue-tier CSV and strict loader contract</name>
  <files>
    data/assets/ai_cs_venue_tiers.csv
    pipeline/venue_tiers.py
    tests/test_venue_tiers.py
  </files>
  <read_first>
    .planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-RESEARCH.md
    pipeline/raw_staging.py
    scripts/s1_openalex_fetch.py
    sources/openalex.py
  </read_first>
  <behavior>
    - Test 1: The CSV exposes one explicit Phase 6 schema with upstream CCF/CORE evidence, normalized tier, inclusion flag, and review metadata for each venue row.
    - Test 2: The loader indexes rows by exact `openalex_source_id` and rejects duplicate source IDs, blank required fields, and invalid tier values.
    - Test 3: The contract keeps Bio/Pharma and non-AI/CS rows out of scope and does not introduce fuzzy runtime matching, per CORPUS-02.
  </behavior>
  <action>
    Create `data/assets/ai_cs_venue_tiers.csv` as the repo-owned Phase 6 source of truth. The CSV
    must include exactly these columns: `canonical_venue_slug`, `canonical_venue_name`,
    `venue_kind`, `openalex_source_id`, `openalex_source_display_name`, `ccf_grade`,
    `core_grade`, `normalized_tier`, `include_in_ai_cs_corpus`, `evidence_ccf_url`,
    `evidence_core_url`, `last_reviewed_at`, and `review_notes`.

    Store the exact OpenAlex source identifier string as it appears in staged OpenAlex work payloads
    under `raw.primary_location.source.id`; do not strip it to a name slug and do not introduce a
    secondary venue-name key. Populate the asset with real AI/CS rows needed to cover the current
    staged sample runs plus representative included and excluded AI/CS venues. Do not commit a
    header-only, illustrative, or placeholder table.

    Implement `pipeline/venue_tiers.py` with a strict loader/validator for this CSV. Allow only the
    explicit normalized tiers `T1`, `T2`, `T3`, `T4`, `EXCLUDE`, and `UNRESOLVED`. Require
    `include_in_ai_cs_corpus=true` only for `T1`-`T4`; require `false` for `EXCLUDE` and
    `UNRESOLVED`. Write `tests/test_venue_tiers.py` first and keep the implementation flat Python,
    with no live CCF/CORE fetching and no runtime fuzzy lookup.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_venue_tiers.py -q -k "schema or loader"` passes.
    - `rg '^canonical_venue_slug,canonical_venue_name,venue_kind,openalex_source_id,openalex_source_display_name,ccf_grade,core_grade,normalized_tier,include_in_ai_cs_corpus,evidence_ccf_url,evidence_core_url,last_reviewed_at,review_notes$' data/assets/ai_cs_venue_tiers.csv` returns the header.
    - `rg 'T1|T2|T3|T4|EXCLUDE|UNRESOLVED' data/assets/ai_cs_venue_tiers.csv` shows explicit tier values instead of blanks.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_venue_tiers.py -q -k "schema or loader"</automated>
  </verify>
  <done>
    The repo contains one reviewed AI/CS venue-tier asset keyed by exact OpenAlex source IDs, and
    the loader proves the gate can consume it without any heuristic venue matching.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the reason taxonomy and venue-table fingerprint contract</name>
  <files>
    pipeline/venue_tiers.py
    tests/test_venue_tiers.py
  </files>
  <read_first>
    data/assets/ai_cs_venue_tiers.csv
    pipeline/venue_tiers.py
    tests/test_venue_tiers.py
  </read_first>
  <behavior>
    - Test 1: The module exports the exact Phase 6 decision taxonomy: `matched_ai_cs_venue_tier`, `missing_primary_source`, `source_not_in_ai_cs_table`, `venue_row_unresolved`, and `venue_explicitly_excluded`.
    - Test 2: The venue-table fingerprint is stable for unchanged CSV content and changes when the asset content changes.
    - Test 3: The taxonomy contains only venue-gating reasons; it does not leak ranking, citation, author, or recency logic into Phase 6.
  </behavior>
  <action>
    Extend `pipeline/venue_tiers.py` so it exports the canonical Phase 6 include/exclude reason-code
    constants and a stable `hash_venue_tier_asset(path)` helper computed from the CSV content. This
    fingerprint is the only venue-table version surface downstream manifests may rely on.

    Keep the taxonomy intentionally narrow: it must express only venue-join outcomes. Do not add
    fallback reasons for display-name matching, ranking formulas, work-type scoring, or later-phase
    enrichment. Update `tests/test_venue_tiers.py` first so the reason set and fingerprint behavior
    are locked before the gate runtime begins.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_venue_tiers.py -q -k "reason or fingerprint"` passes.
    - `rg 'matched_ai_cs_venue_tier|missing_primary_source|source_not_in_ai_cs_table|venue_row_unresolved|venue_explicitly_excluded' pipeline/venue_tiers.py` finds the exported reason codes.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_venue_tiers.py -q -k "reason or fingerprint"</automated>
  </verify>
  <done>
    Phase 6 now has one stable decision taxonomy and one stable venue-table fingerprint contract
    that later gate outputs can cite directly.
  </done>
</task>

</tasks>

<verification>
Run `.venv/bin/python -m pytest tests/test_venue_tiers.py -q` and confirm the asset schema,
loader, reason codes, and fingerprint helper all pass offline.
</verification>

<success_criteria>
Phase 6 has one repo-owned AI/CS venue-tier asset keyed by exact OpenAlex source IDs, and the
contract is strict enough that the runtime gate can stay heuristic-free.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-cs-corpus-gate-and-venue-tiers/06-01-SUMMARY.md`
</output>
