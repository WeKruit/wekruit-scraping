---
phase: 01-official-ai-ingest-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config/settings.example.py
  - config/source_registry.py
  - pipeline/run_context.py
  - pipeline/raw_staging.py
  - tests/test_phase1_contracts.py
autonomous: true
requirements:
  - INGEST-03
  - QUALITY-02
must_haves:
  truths:
    - "Phase 1 has one explicit config surface and one source registry that distinguishes primary ingest from metadata backfill."
    - "Every raw staged record can be traced back to a run manifest with source, slice, limits, retries, and replay metadata."
    - "Raw staging preserves source-native payloads instead of prematurely normalizing them."
  artifacts:
    - path: "config/source_registry.py"
      provides: "Source registry with explicit source role, rate-limit policy, and retry policy for OpenAlex and Crossref"
      contains: "openalex"
    - path: "pipeline/run_context.py"
      provides: "Run manifest builder and persistence helpers"
      contains: "RunManifest"
    - path: "pipeline/raw_staging.py"
      provides: "Raw envelope and JSONL staging helpers"
      contains: "raw"
  key_links:
    - from: "config/source_registry.py"
      to: "pipeline/run_context.py"
      via: "selected source config becomes part of each run manifest"
      pattern: "get_source_config"
    - from: "pipeline/run_context.py"
      to: "pipeline/raw_staging.py"
      via: "run_id and manifest metadata are stamped onto staged envelopes"
      pattern: "run_id"
---

<objective>
Lock the Phase 1 execution contract before any source adapter work starts.

Purpose: Give all later Phase 1 code one stable config surface, one source registry, one run
manifest shape, and one raw staging envelope so replay/audit semantics do not drift.
Output: Config example, source registry, run manifest helpers, raw staging helpers, and contract
tests.
</objective>

<execution_context>
@/Users/adam/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/01-official-ai-ingest-foundation/01-CONTEXT.md
@reference/p9-research-pipeline/README.md
@reference/p9-research-pipeline/scripts/s1_openalex_fetch.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Phase 1 config surface and source registry</name>
  <files>
    requirements.txt
    config/settings.example.py
    config/source_registry.py
    tests/test_phase1_contracts.py
  </files>
  <behavior>
    - Test 1: The source registry marks OpenAlex as the primary ingest source and Crossref as backfill-only, per D-01 and D-02.
    - Test 2: Registry entries expose rate-limit, retry, and polite-contact settings needed for Phase 1 reruns, per D-03.
    - Test 3: The config example exposes only the minimal Phase 1 settings for OpenAlex and Crossref, and does not introduce framework or database config, per D-05.
  </behavior>
  <action>
    Create `requirements.txt` with the minimal Phase 1 runtime/test dependencies: `pyalex`,
    `habanero`, `requests`, and `pytest`. Then create `config/settings.example.py` and
    `config/source_registry.py`.

    In `settings.example.py`, define only Phase 1 settings: root data directory, polite contact
    email(s), optional OpenAlex API key, default retry counts, and default per-run limits. Do not
    add settings for Phase 2+ sources or any service/framework configuration, per D-05 and D-06.

    In `source_registry.py`, define an explicit registry for `openalex` and `crossref` only. The
    registry must encode each source's role (`primary_ingest` vs `metadata_backfill`), retry
    policy, and polite request metadata. Add a tiny accessor/validation surface so later scripts can
    fetch one source config by name instead of hardcoding source behavior in CLIs.

    Write `tests/test_phase1_contracts.py` first. Use fixture-driven assertions to lock registry
    semantics and to prove that Crossref cannot be treated as a standalone discovery source in Phase
    1, per D-02. Keep the implementation flat and importable from scripts without adding a
    framework.
  </action>
  <verify>
    <automated>pytest tests/test_phase1_contracts.py -q -k "registry or settings"</automated>
  </verify>
  <done>
    `requirements.txt`, `settings.example.py`, and `source_registry.py` exist; tests prove the
    registry semantics match Phase 1 decisions; no Phase 2+ source or framework config leaks into
    the files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define run manifest and raw staging contract</name>
  <files>
    pipeline/run_context.py
    pipeline/raw_staging.py
    tests/test_phase1_contracts.py
  </files>
  <behavior>
    - Test 1: Building a run manifest for the same source + slice + limits yields stable identity fields and captures retries/limits needed for replay, per D-03.
    - Test 2: Raw staging wraps a source-native payload inside an envelope that preserves the untouched raw record plus run metadata.
    - Test 3: The staging helpers emit the Phase 1 directory layout for run manifests and raw JSONL without introducing canonical-schema fields from Phase 2, per D-06.
  </behavior>
  <action>
    Implement `pipeline/run_context.py` with a small manifest model and helpers that build a
    `run_id`, capture source name, slice kind/value, query hash, limits, retries, created
    timestamp, and parent run reference if applicable. Persist manifests under a simple file-backed
    path such as `data/runs/<run_id>.json`.

    Implement `pipeline/raw_staging.py` with helpers that stage raw JSONL envelopes under a
    deterministic directory layout such as `data/raw/<source>/<run_id>/<entity>.jsonl`. Each
    envelope must keep source-native payloads under a `raw` field and add only Phase 1 metadata:
    `source`, `entity_type`, `source_record_id`, `run_id`, `slice`, and fetch timestamp. Do not
    normalize fields into the canonical researcher record yet, per D-06.

    Expand `tests/test_phase1_contracts.py` first to cover manifest generation, raw envelope shape,
    and persisted-path behavior. Keep the contract simple and replayable: file-based state only, no
    database, no queue, no framework, per D-05.
  </action>
  <verify>
    <automated>pytest tests/test_phase1_contracts.py -q -k "run_manifest or raw_staging"</automated>
  </verify>
  <done>
    Run manifests and raw staging helpers exist, tests prove raw payloads remain source-native, and
    Phase 1 now has a stable replay/audit contract that later plans can consume directly.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_phase1_contracts.py -q` and confirm the contract suite passes without any
live network dependency.
</verification>

<success_criteria>
Phase 1 has one shared config/registry surface, one run manifest contract, and one raw staging
contract that later source adapters can use without redefining replay or audit semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-official-ai-ingest-foundation/01-01-SUMMARY.md`
</output>
