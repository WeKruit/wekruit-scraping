---
phase: 01-official-ai-ingest-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - presets/ai_ml.py
  - sources/openalex.py
  - scripts/s1_openalex_fetch.py
  - tests/test_openalex_ingest.py
  - tests/fixtures/openalex/works_page.json
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
must_haves:
  truths:
    - "User can run one OpenAlex-backed Phase 1 ingest for an AI/ML venue, concept, or keyword preset."
    - "The ingest stages raw work and author records through the shared Phase 1 run/staging contract."
    - "Preset selection is explicit and reusable instead of hardcoded inside the fetch script."
  artifacts:
    - path: "presets/ai_ml.py"
      provides: "AI/ML venue, concept, and keyword presets"
      contains: "venue"
    - path: "sources/openalex.py"
      provides: "OpenAlex query/build/fetch helpers aligned to Phase 1"
      contains: "paginate"
    - path: "scripts/s1_openalex_fetch.py"
      provides: "Executable OpenAlex ingest CLI that writes staged works/authors"
      contains: "argparse"
  key_links:
    - from: "presets/ai_ml.py"
      to: "scripts/s1_openalex_fetch.py"
      via: "CLI preset selection resolves into one preset definition"
      pattern: "preset"
    - from: "sources/openalex.py"
      to: "pipeline/raw_staging.py"
      via: "fetched works and deduped authors are staged through shared raw envelope helpers"
      pattern: "stage"
---

<objective>
Implement the Phase 1 primary ingest path on top of OpenAlex and make AI/ML slicing explicit.

Purpose: Turn the shared Phase 1 contract into a real official-source ingest that can fetch and
stage AI/ML works/authors without generic crawling or second-primary-source drift.
Output: AI/ML presets, OpenAlex adapter, executable CLI, and fixture-backed tests.
</objective>

<execution_context>
@/Users/adam/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/01-official-ai-ingest-foundation/01-CONTEXT.md
@reference/p9-research-pipeline/README.md
@reference/p9-research-pipeline/scripts/s1_openalex_fetch.py

<interfaces>
Expected from `pipeline/run_context.py` after `01-01`:

```python
class RunManifest: ...
def build_run_manifest(...): ...
def write_run_manifest(...): ...
```

Expected from `pipeline/raw_staging.py` after `01-01`:

```python
def stage_records(...): ...
def stage_record(...): ...
```

Use those shared contracts instead of inventing new OpenAlex-specific staging shapes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define AI/ML presets and OpenAlex adapter surface</name>
  <files>
    presets/ai_ml.py
    sources/openalex.py
    tests/test_openalex_ingest.py
    tests/fixtures/openalex/works_page.json
  </files>
  <behavior>
    - Test 1: Venue, concept, and keyword presets all resolve to explicit AI/ML slice definitions, per D-04.
    - Test 2: The OpenAlex adapter builds query/filter inputs from one preset definition and keeps OpenAlex as the only primary discovery source, per D-01.
    - Test 3: Adapter parsing returns staged work candidates plus deduped author candidates from authorships without prematurely normalizing to the Phase 2 canonical schema, per D-06.
  </behavior>
  <action>
    Create `presets/ai_ml.py` with named presets for the three required slice families: venue,
    concept, and keyword. Keep the preset surface explicit and repo-owned, for example a dict or
    lightweight dataclass map keyed by preset slug. Include only AI/ML-focused presets in Phase 1,
    per D-04 and D-06.

    Create `sources/openalex.py` as a thin adapter around `pyalex` or plain response dicts. It
    should translate one preset definition into OpenAlex filters/search parameters, page through
    works, and extract two raw streams: source-native work records and deduped author records
    derived from work authorships. Do not add Crossref behavior here and do not add any generic
    crawling fallback, per D-01 and D-07.

    Write `tests/test_openalex_ingest.py` first and back it with
    `tests/fixtures/openalex/works_page.json`. Keep tests fully offline by loading the fixture
    and/or monkeypatching the adapter boundary.
  </action>
  <verify>
    <automated>pytest tests/test_openalex_ingest.py -q -k "preset or adapter"</automated>
  </verify>
  <done>
    AI/ML presets exist for venue/concept/keyword slices, the OpenAlex adapter consumes them, and
    tests prove the adapter emits work/author raw candidates without introducing Phase 2 logic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the OpenAlex ingest CLI on top of the shared staging contract</name>
  <files>
    scripts/s1_openalex_fetch.py
    sources/openalex.py
    tests/test_openalex_ingest.py
  </files>
  <behavior>
    - Test 1: The CLI accepts an AI/ML preset selection plus Phase 1 run controls (`since`, max records/pages, output root) and builds one run manifest.
    - Test 2: The CLI stages raw works and raw authors using the shared `01-01` helpers instead of writing ad-hoc JSONL.
    - Test 3: Running the CLI against mocked OpenAlex responses produces deterministic run output paths and does not depend on Crossref or any later-phase source.
  </behavior>
  <action>
    Implement `scripts/s1_openalex_fetch.py` as the single OpenAlex Phase 1 entrypoint. Use
    argparse. Support a preset-family + preset-name interface such as
    `--preset-type venue --preset neurips` (or an equally explicit equivalent), plus `--since`,
    `--max-records`/`--max-pages`, and an optional output root override. The script must create one
    run manifest, call the OpenAlex adapter, and stage raw `works` and `authors` outputs through the
    shared raw-staging helpers from `01-01`.

    Keep the script aligned with the repo and the reference naming (`s1_openalex_fetch.py`), but
    strip it down to the shortest correct path: no top-author secondary enrichment pass, no generic
    search fallback, no later-phase normalization logic, and no framework bootstrap, per D-05 and
    D-06.

    Extend `tests/test_openalex_ingest.py` first so it covers the CLI boundary with monkeypatched
    adapter/staging helpers. The tests should verify that venue, concept, and keyword presets all
    flow through the same run-manifest/staging contract.
  </action>
  <verify>
    <automated>pytest tests/test_openalex_ingest.py -q -k "cli or staging" && python scripts/s1_openalex_fetch.py --help >/dev/null</automated>
  </verify>
  <done>
    `s1_openalex_fetch.py` exists, stages raw works/authors for AI/ML presets through the shared
    contract, and the offline tests prove the CLI works for all three preset families.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_openalex_ingest.py -q` and confirm the OpenAlex ingest can be exercised
entirely through fixtures/monkeypatches.
</verification>

<success_criteria>
Phase 1 now has a real official-source ingest path: one OpenAlex CLI that stages AI/ML work/author
raw outputs from venue, concept, or keyword presets using the shared run/staging contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01-official-ai-ingest-foundation/01-02-SUMMARY.md`
</output>
