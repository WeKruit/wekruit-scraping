---
phase: 01-official-ai-ingest-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - pipeline/incremental_state.py
  - sources/crossref.py
  - scripts/s1_crossref_backfill.py
  - scripts/s1_openalex_fetch.py
  - tests/test_crossref_backfill.py
  - tests/fixtures/crossref/work.json
autonomous: true
requirements:
  - INGEST-03
  - QUALITY-02
must_haves:
  truths:
    - "Crossref can enrich DOI-backed OpenAlex records without acting as a second primary discovery path."
    - "OpenAlex and Crossref runs can resume or replay through file-backed incremental controls while preserving raw record semantics."
    - "Run manifests and staged raw files keep parent-run lineage needed for audit."
  artifacts:
    - path: "sources/crossref.py"
      provides: "Crossref DOI metadata backfill adapter"
      contains: "doi"
    - path: "pipeline/incremental_state.py"
      provides: "File-backed replay and incremental checkpoint helpers"
      contains: "checkpoint"
    - path: "scripts/s1_crossref_backfill.py"
      provides: "Executable Crossref backfill CLI tied to staged OpenAlex runs"
      contains: "input-run"
  key_links:
    - from: "scripts/s1_crossref_backfill.py"
      to: "scripts/s1_openalex_fetch.py"
      via: "Crossref consumes DOI-bearing work records produced by OpenAlex runs"
      pattern: "input_run"
    - from: "pipeline/incremental_state.py"
      to: "pipeline/run_context.py"
      via: "checkpoint and parent-run metadata extend the existing run manifest contract"
      pattern: "parent_run_id"
---

<objective>
Finish the Phase 1 ingest foundation by adding DOI backfill and file-backed rerun controls.

Purpose: Keep OpenAlex as the backbone while making Crossref useful for DOI metadata enrichment and
ensuring Phase 1 reruns are replayable, resumable, and audit-safe.
Output: Crossref adapter, Crossref backfill CLI, incremental/replay helpers, and offline tests.
</objective>

<execution_context>
@/Users/adam/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/01-official-ai-ingest-foundation/01-CONTEXT.md
@reference/p9-research-pipeline/README.md

<interfaces>
Expected from `scripts/s1_openalex_fetch.py` after `01-02`:

```python
def main(): ...
```

Expected behavior from prior plans:
- OpenAlex raw works are staged with stable `source_record_id` values and a persisted `run_id`.
- Run manifests already exist and can be extended with parent-run and checkpoint metadata.

Do not replace those contracts; extend them minimally for Crossref backfill and replay support.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Crossref DOI metadata backfill against staged OpenAlex runs</name>
  <files>
    sources/crossref.py
    scripts/s1_crossref_backfill.py
    tests/test_crossref_backfill.py
    tests/fixtures/crossref/work.json
  </files>
  <behavior>
    - Test 1: Crossref backfill requires an upstream OpenAlex run as input and rejects use as a standalone discovery entrypoint, per D-02.
    - Test 2: The adapter fetches DOI metadata politely and stages raw Crossref records without mutating the staged OpenAlex record shape, per D-03.
    - Test 3: Records without DOI are skipped cleanly and tracked in run/report metadata instead of forcing a fallback path, per D-07.
  </behavior>
  <action>
    Create `sources/crossref.py` as a thin DOI lookup adapter around `habanero` (or an equally thin
    `requests` wrapper if that keeps the code clearer). It must accept one DOI at a time and return
    source-native Crossref response payloads plus the minimal lookup metadata needed for audit.

    Create `scripts/s1_crossref_backfill.py` as the Phase 1 backfill entrypoint. It must require an
    upstream OpenAlex run identifier or raw-work path, read staged OpenAlex works, extract DOIs,
    skip records with no DOI, fetch Crossref metadata, and stage raw Crossref payloads under the
    shared raw staging contract. Persist explicit parent-run lineage so audit can trace a Crossref
    record back to the OpenAlex run that surfaced the DOI, per D-02 and D-03.

    Write `tests/test_crossref_backfill.py` first using `tests/fixtures/crossref/work.json` and
    monkeypatched DOI lookups. Keep the tests offline. Do not let this script fetch authors or
    works directly from Crossref search endpoints, because that would violate D-02.
  </action>
  <verify>
    <automated>pytest tests/test_crossref_backfill.py -q -k "crossref or doi"</automated>
  </verify>
  <done>
    Crossref backfill exists only as DOI enrichment over staged OpenAlex works, raw Crossref payloads
    are staged with parent-run lineage, and tests prove it cannot be used as a second discovery path.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add replay and incremental controls without changing record semantics</name>
  <files>
    pipeline/incremental_state.py
    scripts/s1_openalex_fetch.py
    scripts/s1_crossref_backfill.py
    tests/test_crossref_backfill.py
  </files>
  <behavior>
    - Test 1: OpenAlex reruns can resume from stored checkpoint state or replay an existing run without changing raw envelope shape, per D-03.
    - Test 2: Crossref backfill can skip already-processed DOIs for the same input lineage and resume from file-backed checkpoint state, per QUALITY-02.
    - Test 3: Incremental controls stay file-backed and repo-aligned rather than introducing a database or orchestration layer, per D-05.
  </behavior>
  <action>
    Implement `pipeline/incremental_state.py` as the single file-backed helper for replay and resume
    behavior. Keep it minimal: persisted checkpoints and seen-ID/seen-DOI helpers stored under
    `data/state/` or an equally explicit Phase 1 path.

    Extend `scripts/s1_openalex_fetch.py` to support replay/resume controls such as
    `--resume-run <run_id>` and/or a deterministic equivalent that continues the same query lineage.
    Extend `scripts/s1_crossref_backfill.py` to support the same pattern for DOI backfills. The key
    invariant is that reruns may change fetch bookkeeping and staged coverage, but they must not
    change the raw record envelope contract introduced in `01-01`.

    Update `tests/test_crossref_backfill.py` first so it locks replay/incremental semantics for
    both scripts. Keep the implementation file-backed only; do not add a database, queue, or
    scheduler, per D-05.
  </action>
  <verify>
    <automated>pytest tests/test_crossref_backfill.py -q -k "resume or replay or incremental" && python scripts/s1_openalex_fetch.py --help >/dev/null && python scripts/s1_crossref_backfill.py --help >/dev/null</automated>
  </verify>
  <done>
    OpenAlex and Crossref both support replay/resume through one file-backed state helper, and the
    tests prove reruns preserve the raw envelope semantics defined in `01-01`.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/test_crossref_backfill.py -q` and confirm both Crossref backfill semantics and
replay/incremental controls pass offline.
</verification>

<success_criteria>
Phase 1 ends with OpenAlex as the backbone, Crossref as DOI backfill, and replay/incremental
controls that keep raw staging deterministic and auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-official-ai-ingest-foundation/01-03-SUMMARY.md`
</output>
